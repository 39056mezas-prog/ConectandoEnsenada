/**
 * UI Component Library — ConectandoEnsenada.org
 *
 * All base UI primitives are exported from here.
 * Import from '@/components/ui' rather than individual files.
 *
 * Example:
 *   import { Button, Badge, Skeleton } from '@/components/ui'
 *
 * Components will be added progressively as modules are built.
 * Phase 1 ships only the primitives needed for Sprint 1-2.
 */

export { Button } from './button'
export { Badge, VerifiedBadge, FeaturedBadge } from './badge'
export { Skeleton, PlaceCardSkeleton, ArticleCardSkeleton } from './skeleton'
